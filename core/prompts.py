"""
Prompt 模板模块

职责：
- 集中管理所有 AI 提示词模板
- 任务书编排、分析、更新的 prompt 定义
"""


TASKBOOK_ORGANIZE_PROMPT = """
你是一个项目管理专家。请将以下原始任务内容整理成 **固定结构** 的 Markdown 任务书，供网页解析展示。

## 原始任务内容
{content}

## 必须遵守的输出结构（机器可读）

1. 第一行起直接输出 Markdown，不要代码围栏，不要前言后语。
2. 顶层标题固定为：`# 项目任务书`
3. 每个任务条目 **必须** 使用二级标题，格式严格为：`## 数字. 简短标题`
   - 数字从 1 起连续递增（1. 2. 3. …）。
4. 在每个 `## 数字. 标题` 标题下的正文里，**必须**包含以下行（使用 Markdown 加粗标签，冒号为中文冒号）：
   - `- **条目：**` 后写该条目的名称或一句话描述（可与标题一致或更具体）。
   - `- **具体完成了什么内容：**` 后写当前已知进展；若尚不可知则写「待补充」或「未开始」（网页折叠区「完成情况」读取本字段；也可额外写 `- **完成情况：**` 二选一即可）。
   - 可选：`- **贡献：**` 主要贡献者、提交说明或协作信息；若无则**不要写该行**（网页「贡献」区将留空）。
   - 可选：`- **关联路径：**` 后写仓库内相关文件或目录路径（单一路径或逗号分隔）。
5. 条目之间不要插入三级标题打乱编号；如需补充说明，使用普通段落写在上述字段之后。

## 输出示例（结构示意，勿照抄内容）

# 项目任务书

## 1. 用户登录

- **条目：** 用户登录功能
- **具体完成了什么内容：** 待补充
- **关联路径：** `src/auth/login.py`

## 2. 注册流程

- **条目：** 用户注册
- **具体完成了什么内容：** 未开始

请根据原始材料提炼条目数量与标题；能合并的合并，能拆分的拆分，保持条目清晰。

请直接输出整理后的完整 Markdown 任务书，不要添加任何说明文字。
"""


TASK_ANALYSIS_PROMPT = """你是一个代码任务分析专家。请根据代码变更分析任务完成情况。

## 分析基准时刻（任务书追加日志时必须使用）
{current_time_zh}
（写入任务书「具体完成了什么内容」的**每一条新增记录**，时间戳须与此完全一致：前缀格式 `【{current_time_zh}】`，精确到小时。）

## 仓库信息
名称: {repo_name}
描述: {repo_desc}

## 待分析任务 ({task_count}个)
{tasks_summary}

## 代码变更 ({file_count}个文件)
{changes_summary}

## 贡献统计 ({member_count}人)
{members_summary}

## 关于「review」的常见层次（供你判断变更性质，不必在 JSON 中复述）
- **人工 Code Review**：人读 diff/PR，关注逻辑、边界、安全、可读性。
- **Diff / 提交粒度审阅**：按 commit 或 compare 视图看文件级、块级变更。
- **自动化审阅**：Lint、类型检查、单测、SAST 等规则驱动。
- **策略/噪声过滤**：区分「影响产品行为的实质变更」与「维护性、配置、文案微调」。

## 分析要求
根据文件变更分析每个任务的完成情况。

### 完成度计算规则
- 相关文件有修改/新增: +5-10% (根据变更数量)
- 相关文件有删除: -5% (可能重构)
- 无相关变更: 保持0%或当前值
- 完成度范围: 0-100%

### 状态判断
- 100%: "completed"
- 1-99%: "in_progress"
- 0%: "not_started"

### 匹配优先级
1. 文件路径完全匹配
2. 文件路径包含任务相关路径
3. 考虑作者贡献活跃度

### 任务书「完成情况」噪声控制（极其重要）
下列类型通常 **不应** 再写入任务书条目的长篇详情（`taskbook_detail_mode` 取 `skip`，且 `completion_log_line` 必须为空字符串 `""`）：
- 仅配置/常量微调：如改端口、改 `web_url`、开关默认值、环境变量名不变义等 **未改变业务语义** 的改动。
- 纯注释、排版、换行、无行为变化的格式化；仅 README/文档措辞微调且与条目目标无关。
- 与某条目「关联路径」明显无关的边角文件改动。

若变更 **确实推进** 了该条目对应能力（新逻辑、可测行为、接口契约、关键 bug 修复等），则 `taskbook_detail_mode` 取 `append`，并在 `completion_log_line` 写 **一条** 简洁中文记录，且 **必须以** `【{current_time_zh}】` **开头**，后接事实描述（可引用路径/行为），不要堆砌无关文件列表。

## 输出格式
必须且只能返回 JSON，不要其他文字：

```json
{{
  "tasks": [
    {{
      "name": "任务名称",
      "completion_rate": 数字0-100,
      "status": "completed|in_progress|not_started",
      "main_contributor": "作者姓名或空字符串",
      "evidence": "简短技术判断依据",
      "taskbook_detail_mode": "skip 或 append",
      "completion_log_line": "若 append：必须以【{current_time_zh}】开头的单行追加文案；若 skip：必须为空字符串"
    }}
  ]
}}
```

## 重要
1. completion_rate 必须是纯数字，不要%
2. status 只能是: completed/in_progress/not_started
3. taskbook_detail_mode 只能是 skip 或 append
4. 只返回 JSON，不要任何说明文字
"""


TASKBOOK_UPDATE_PROMPT = """请根据「任务状态更新」改写任务书，并 **严格保持** 既有 Markdown 结构。

## 当前任务书
{current_content}

## 任务状态更新（JSON，含 taskbook_detail_mode / completion_log_line）
{status_summary}

## 本次分析基准时刻（写入详情时必须一致）
{current_time_zh}
凡向「具体完成了什么内容」或「完成情况」**新增**一句进展描述，该行/该段必须以 `【{current_time_zh}】` 开头（精确到小时，与 JSON 中 completion_log_line 前缀一致）。禁止省略时间戳。

## 格式保持（必须）

若当前任务书已采用「网页固定格式」，则必须继续遵守：

- 顶层为 `# 项目任务书`
- 每个任务块以二级标题 `## 数字. 标题` 开头，数字连续
- 每个块内保留字段行（加粗 + 中文冒号）：
  - `- **条目：**` …
  - `- **具体完成了什么内容：**` 或 `- **完成情况：**` …
  - 若有贡献信息，保留 `- **贡献：**` …；若无则省略该行
  - 若有路径信息，保留 `- **关联路径：**` …

若当前任务书仍是旧版 checklist（`- [ ]`），则仅在本次更新中 **整体迁移** 为上述固定格式后再写入结论。

## 与 JSON 对齐的更新规则（极其重要）

1. 对上表 JSON 中 **每一条** `tasks[]`：
   - 若 `taskbook_detail_mode` 为 **skip**：**不要**因本次微调去改写或拉长该条目对应的「具体完成了什么内容 / 完成情况」；保持原文（若原为「待补充」「未开始」亦保持），避免把配置名微调、无关 diff 写进任务书造成杂乱。
   - 若 `taskbook_detail_mode` 为 **append** 且 `completion_log_line` 非空：在对应条目的「具体完成了什么内容」（或「完成情况」）字段中 **追加**该字符串（已是 `【{current_time_zh}】` 开头的一条）；若已有历史记录，用换行分隔多条，**每条各自带时间戳**。
2. 不得把 `skip` 条目的 evidence 抄进任务书详情。
3. 仍可根据 completion_rate / status 在文内做极简状态用语（如「进行中」），但不要与上一条冲突。

## 状态与文字建议

- 已完成：除 append 规则外，可保留原有总结性句子。
- 进行中：写清剩余工作；新增进展仅用带时间戳的 append 行。
- 未开始：保持「未开始」或「待补充」，除非 append 明确要求写入新事实。

## 输出要求

**只返回更新后的完整任务书 Markdown，不要代码围栏，不要任何说明文字。**
"""
